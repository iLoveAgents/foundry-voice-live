# Lefthook configuration
# https://lefthook.dev/configuration/

pre-commit:
  parallel: true
  jobs:
    - name: lint
      glob: "*.{ts,tsx}"
      run: pnpm -r run lint

    - name: typecheck
      run: pnpm -r run typecheck

    - name: test
      run: pnpm -r run test
