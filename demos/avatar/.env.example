# Proxy URL - Default is local proxy server
# Production: use your deployed proxy URL (e.g., wss://yourapp.com/ws?model=gpt-realtime)
# VITE_PROXY_URL=ws://localhost:8080/ws?model=gpt-realtime

# Avatar Configuration (Optional - override defaults)
# VITE_AVATAR_CHARACTER=lisa
# VITE_AVATAR_STYLE=casual-standing

# Legacy Direct Connection (NOT RECOMMENDED for production - exposes API keys in browser)
# Only use for local development/testing if you don't want to run the proxy
# VITE_AZURE_SPEECH_KEY=your-azure-ai-services-key
# VITE_AZURE_AI_FOUNDRY_RESOURCE=aif-atw-ttsavatar-dev
